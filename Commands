sudo gem install jekyll             install Jekyll=
jekyll -v                           check if functional
jekyll new icarlyle.github.io       make jekyll blog
jekyll serve                        start jekyll server